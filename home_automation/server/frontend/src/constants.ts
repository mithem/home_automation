export const BASE_URL = document.location.protocol + "//" + document.location.hostname + ":" + document.location.port
export const refreshInterval = 2 * 1000
export const slowRefreshInterval = 2 * 1000
